{
  "name": "enclave",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run tauri dev",
    "build": "npm run tauri build",
    "check": "npm run check -w @enclave/desktop",
    "tauri": "tauri"
  }
}
