{
  "name": "enclave",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run tauri dev",
    "build": "npm run tauri build",
    "preview": "npm run preview -w @enclave/desktop",
    "check": "npm run check -w @enclave/desktop",
    "tauri": "npm run tauri -w @enclave/desktop"
  }
}
